# DockerCompose-Windows-NAT-Java
This templete of docker-compose.yml automatically creates a NAT network and container in Windows Server 2019 (and higher).

## Run Example
docker-compose.exe -f docker-compose.yml up -d